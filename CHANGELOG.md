# Changelog

本文件记录插件的重要变更。版本号与 `metadata.yaml` 保持一致，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格。

## [v1.2.0] - 2026-08-06

### 新增

- 球员经纪人档位（温和 / 普通 / 苛刻）：球员库成员具有档位属性，`/待谈判` 列表展示经纪人档位
- 报价直接失败机制：报价过低时存在直接失败风险，触发后谈判立即终止并按该次预期工资结算
- 经纪人档位配置项（`agent_tiers`、`agent_change_probability`），非法配置运行时自动回退默认值
- 赛季 / 转会窗口推进时按概率重掷球员经纪人档位

### 变更

- 数据库 schema v4 → v5：`player_roster` 新增 `agent_tier` 列，旧库自动迁移（默认普通档）
- `/谈判查看配置` 隐藏内部参数（工资计算与档位参数），避免误改

## [v1.1.1] - 2026-08-04

### 新增

- 报价失败反馈把握分档（四档）：🥶 希望渺茫 / 🤔 有一定机会 / 😄 机会较大 / 🎉 几乎必成
- 把握分档阈值配置化（`tier_thresholds`），`/谈判设置` 校验非法值

### 修复

- 代码审查发现的 13 项健壮性问题：commit 失败重试前 rollback、解绑事务化、绑定锁剪枝逻辑、导入解析异步化、配置文件死路径等
- 关闭窗口令牌流程回归

### 变更

- 插件名与展示名更新为 WHL 品牌

## [v1.1.0] - 2026-08-04

### 变更

- 解绑单参数化：`/解绑 <QQ|队名>`，队内多人时警告要求指定 QQ
- `/创建谈判` 队名参数前置；`/绑定球队` 仅需认证码
- 统一 `/命名 <窗口|赛季> <名称>`，历史名字永久保留，展示处名字优先、无名字降级为序号
- 新增 `seasons` 表（schema v1 → v4 迁移链 + 幂等回填）
- 推进 / 关闭 / 命名在事务内重读状态（修复并发窗口滞留竞态）
- 报价输入严格 2 位小数校验（修复 1.005 静默变 1.0 问题）
- 绑定失败限次默认调优为 3

## [v1.0.0] - 2026-08-04

### 初始版本

- 球队认证：认证码（8 位 / 1 次有效 / 哈希存储）、一 QQ 一队、管理员解绑
- 球员库：xlsx / csv 批量导入（覆盖更新、编码回退、防路径穿越）
- 工资谈判：违约金区间校验、预期工资快照、递增报价 ≤ 3 次、失败自动成约
- 赛季 / 窗口：连续推进（事务内活跃检查）、成长年龄减一 / 恢复、每日定时备份
- 安全：双群白名单、绑定限流、并发事务原子、竞态不变量
